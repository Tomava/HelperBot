services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - /home/debian/Discord:/home/discord/Discord
      - /media/archive/Discord/archives:/home/discord/Discord/archives
      - /etc/localtime:/etc/localtime
    restart: always
